# AI学習ウィジェットサービス 要件定義・開発ドキュメント

## 1. プロジェクト概要

本プロジェクトは、技術記事の本文をもとに、AIで要約スライドとクイズを生成し、自分のブログ記事ページに埋め込み表示できるサービスを開発するものである。

外部サービス側に記事本文を登録し、その本文をAI生成用のソースとして利用する。生成された要約スライドとクイズは外部サービス側で管理・保存し、ブログ側には埋め込みスクリプトを設置することで表示する。

MVPでは、自分のみが利用する前提とし、複数ユーザー対応・課金・外部サービス連携などは行わない。

---

## 2. 背景

技術記事を書いているが、記事は公開後に読まれて終わりになりやすい。

記事内容を要約スライドやクイズに変換できれば、自分自身の学習定着や、記事を読んだ読者の理解補助に活用できる。

まずは自分の記事を対象に、記事本文を手動で外部サービスへ登録し、AIによって学習コンテンツを生成する。生成したコンテンツは、自分のブログの記事詳細ページに埋め込んで表示できるようにする。

---

## 3. 目的

### 3.1 開発者としての目的

- AIを活用したWebサービス開発を学ぶ
- 記事本文から構造化されたコンテンツを生成する仕組みを学ぶ
- 外部サイトにスクリプトを埋め込んでUIを表示する仕組みを学ぶ
- 小さなCMS的な管理画面を設計・実装する
- 自分の技術記事を学習コンテンツとして再活用する

### 3.2 サービスとしての目的

- 技術記事を読んで終わりにせず、復習・理解度確認に活用できるようにする
- 記事内容をスライド形式で短時間に振り返れるようにする
- 記事内容をもとにしたクイズで理解度を確認できるようにする
- ブログ記事本文とは別に、補助的な学習コンテンツを簡単に追加できるようにする

---

## 4. 解決したい課題

- 技術記事を書いたあと、内容を再活用する機会が少ない
- 長い記事の場合、要点を振り返りづらい
- 記事を読んだあとに理解度を確認する手段がない
- 要約スライドやクイズを手作業で作るのは手間がかかる
- ブログ本体にAI生成機能や管理機能を直接持たせたくない
- 外部サービスとして生成・管理・配信を分離したい

---

## 5. 想定ユーザー

### 5.1 MVP時点

- 自分のみ

### 5.2 将来的な想定ユーザー

- 技術記事を書いているエンジニア
- 個人ブログを運営している開発者
- 技術記事を学習コンテンツとして再活用したい人
- 記事を読んだあとに復習・理解度確認をしたい読者

---

## 6. 前提・決定事項

MVPでは以下を前提とする。

- 外部サービスとして開発する
- 記事本文は外部サービス側に手動で貼り付ける
- 外部サービス側の記事本文は、AI生成に使うための「生成用スナップショット」として扱う
- ブログ側の記事本文と外部サービス側の記事本文は同期しない
- ブログ記事を更新した場合、必要に応じて外部サービス側の本文も手動で更新し、再生成する
- 生成するコンテンツは以下の2種類とする
  - 要約スライド
  - クイズ

- 利用者は一旦、自分のみとする
- 埋め込み先は自分のブログを想定する
- AI生成は記事詳細画面のボタン操作で実行する

---

## 7. 全体構成

```txt
外部サービス
  ├─ 管理画面
  │   ├─ 記事一覧
  │   ├─ 記事作成
  │   ├─ 記事詳細
  │   ├─ スライド生成
  │   ├─ クイズ生成
  │   └─ 埋め込みコード発行
  │
  ├─ API
  │   ├─ 記事管理API
  │   ├─ AI生成API
  │   └─ 埋め込み表示用API
  │
  └─ embed.js
      └─ ブログ記事ページに読み込ませるJS

自分のブログ
  └─ 記事詳細ページ
      └─ embed.js を読み込み、生成済みスライド・クイズを表示
```

---

## 8. 現状フロー

```txt
1. 自分が技術記事を書く
2. 自分のブログに記事を公開する
3. 読者が記事を読む
4. 記事は読まれて終わりになりやすい
5. 要約やクイズを作る場合は手作業になる
```

---

## 9. 理想フロー

```txt
1. 自分が外部サービスにログインする
2. 記事タイトル、元記事URL、記事本文を登録する
3. 記事詳細画面でスライド生成ボタンを押す
4. AIが記事本文をもとに要約スライドを生成する
5. 記事詳細画面でクイズ生成ボタンを押す
6. AIが記事本文をもとにクイズを生成する
7. 生成されたスライド・クイズを保存する
8. 記事ごとの埋め込みコードをコピーする
9. 自分のブログの記事詳細ページに埋め込みコードを設置する
10. ブログ記事上で要約スライドとクイズが表示される
```

---

## 10. ユースケース

### 10.1 管理者としての自分

- 自分は、記事を再活用するために、記事タイトル・元記事URL・記事本文を登録したい
- 自分は、登録した記事本文から要約スライドを生成したい
- 自分は、登録した記事本文から理解度確認用のクイズを生成したい
- 自分は、生成済みのスライドとクイズを記事詳細画面で確認したい
- 自分は、記事ごとの埋め込みコードを取得したい
- 自分は、取得した埋め込みコードを自分のブログ記事ページに設置したい

### 10.2 記事読者

- 読者は、記事の要点をスライド形式で確認したい
- 読者は、記事を読んだあとにクイズで理解度を確認したい
- 読者は、クイズ回答後に正解・不正解と解説を確認したい

---

## 11. MVP範囲

### 11.1 やること

- 自分だけがアクセスできる管理画面を作る
- 記事タイトル・元記事URL・記事本文を登録できる
- 登録済み記事の一覧を確認できる
- 登録済み記事の詳細を確認できる
- 記事詳細画面から要約スライドを生成できる
- 記事詳細画面からクイズを生成できる
- 生成されたスライドとクイズを保存できる
- 記事ごとの埋め込みコードを表示できる
- 埋め込みコードをブログ記事に設置すると、生成済みスライドとクイズを表示できる
- クイズに回答すると、正誤と解説を表示できる

### 11.2 やらないこと

- URLからの記事本文自動取得
- Qiita/Zenn/Notion/GitHubなど外部サービス連携
- ブログ本文との自動同期
- 複数ユーザー対応
- チーム管理
- 課金機能
- クイズ回答履歴の保存
- 読者ごとのスコア管理
- 詳細な分析機能
- スライドやクイズの手動編集
- デザインテーマのカスタマイズ
- 埋め込み先ドメイン制限
- 高度な権限管理

---

## 12. 機能要件

## 12.1 認証機能

### 概要

管理画面は自分だけが利用できるようにする。

### 要件

- 未ログインユーザーは管理画面にアクセスできない
- ログイン済みでも、許可されたメールアドレス以外は管理画面にアクセスできない
- 管理系APIは認証済みユーザーのみ実行できる
- 埋め込み表示用APIは、公開用データのみ認証なしで取得できる

### 推奨

- Googleログインを利用する
- 自分のメールアドレスのみ許可する

---

## 12.2 記事管理機能

### 概要

AI生成に使うための記事データを外部サービス側に登録・管理する。

このサービスにおける記事本文は、ブログ記事の正式な本文ではなく、AI生成に使うためのスナップショットとして扱う。

### 要件

- 管理者は記事を作成できる
- 管理者は記事タイトルを登録できる
- 管理者は元記事URLを登録できる
- 管理者は記事本文を手動で貼り付けて登録できる
- 管理者は登録済みの記事一覧を確認できる
- 管理者は登録済みの記事詳細を確認できる
- 管理者は記事を更新できる
- 管理者は記事を削除できる

### 入力項目

```txt
title: 記事タイトル
sourceUrl: 元記事URL
sourceBody: AI生成に使う記事本文
```

---

## 12.3 要約スライド生成機能

### 概要

登録された記事本文をもとに、AIで要約スライドを生成する。

### 要件

- 管理者は記事詳細画面からスライド生成を実行できる
- スライド生成はボタン操作で実行する
- 記事本文をもとに複数枚のスライドを生成する
- 各スライドはタイトルと本文を持つ
- 生成されたスライドは記事に紐づけて保存する
- 既にスライドが存在する場合、再生成時は既存データを置き換える
- 生成中・生成成功・生成失敗の状態を管理する
- 埋め込み先では、スライドを順番に表示できる
- 読者は次へ・前へ操作でスライドを閲覧できる

### スライド形式

```ts
type Slide = {
  id: string;
  articleId: string;
  order: number;
  title: string;
  content: string;
  createdAt: string;
  updatedAt: string;
};
```

---

## 12.4 クイズ生成機能

### 概要

登録された記事本文をもとに、AIで理解度確認用のクイズを生成する。

### 要件

- 管理者は記事詳細画面からクイズ生成を実行できる
- クイズ生成はボタン操作で実行する
- 記事本文をもとに複数問のクイズを生成する
- 各クイズは問題文、選択肢、正解、解説を持つ
- 生成されたクイズは記事に紐づけて保存する
- 既にクイズが存在する場合、再生成時は既存データを置き換える
- 生成中・生成成功・生成失敗の状態を管理する
- 読者はクイズに回答できる
- 読者は回答後に正解・不正解を確認できる
- 読者は回答後に解説を確認できる
- MVPではクイズ回答履歴は保存しない

### クイズ形式

```ts
type Quiz = {
  id: string;
  articleId: string;
  order: number;
  question: string;
  choices: string[];
  correctChoiceIndex: number;
  explanation: string;
  createdAt: string;
  updatedAt: string;
};
```

---

## 12.5 埋め込み機能

### 概要

外部サービス側で生成したスライド・クイズを、自分のブログ記事ページに表示する。

### 要件

- 管理者は記事ごとの埋め込みコードを取得できる
- 埋め込みコードは記事詳細画面に表示する
- 埋め込みコードを設置した場所に、要約スライドとクイズを表示する
- 埋め込みスクリプトは記事IDをもとに表示対象コンテンツを取得する
- 埋め込み先ページでは、ログインなしで生成済みコンテンツを閲覧できる
- 埋め込み表示用APIは公開可能な生成済みコンテンツのみ返す
- 埋め込み先ではAI生成処理を実行しない
- 埋め込み先では保存済みのスライド・クイズのみ表示する

### 埋め込みコード例

```html
<div data-learning-widget data-article-id="article_xxx"></div>
<script src="https://your-service.com/embed.js"></script>
```

### 表示処理イメージ

```txt
1. embed.js が読み込まれる
2. data-learning-widget 属性を持つ要素を探す
3. data-article-id を取得する
4. 埋め込み表示用APIから生成済みコンテンツを取得する
5. 対象要素内にスライド・クイズUIを描画する
```

---

## 13. 画面要件

## 13.1 ログイン画面

### パス

```txt
/login
```

### 要件

- Googleログインボタンを表示する
- ログイン済みの場合は記事一覧へ遷移する
- 許可されていないユーザーは管理画面にアクセスできない

---

## 13.2 記事一覧画面

### パス

```txt
/admin/articles
```

### 要件

- 登録済み記事の一覧を表示する
- 新規作成画面への導線を表示する
- 各記事の詳細画面への導線を表示する
- 記事タイトルを表示する
- 元記事URLを表示する
- スライド生成状態を表示する
- クイズ生成状態を表示する
- 更新日時を表示する

### 表示イメージ

```txt
記事一覧

[新規作成]

タイトル              スライド状態      クイズ状態      更新日
React Queryの記事      生成済み          未生成          2026/05/31
Next.jsの記事          未生成            未生成          2026/05/30
```

---

## 13.3 記事作成画面

### パス

```txt
/admin/articles/new
```

### 要件

- 記事タイトルを入力できる
- 元記事URLを入力できる
- 記事本文を貼り付けできる
- 保存ボタンを表示する
- 保存後は記事詳細画面へ遷移する

### 入力項目

```txt
タイトル
元記事URL
記事本文
```

---

## 13.4 記事詳細画面

### パス

```txt
/admin/articles/[id]
```

### 要件

記事詳細画面は以下の情報を表示する。

- 記事タイトル
- 元記事URL
- 記事本文
- スライド生成状態
- クイズ生成状態
- スライド生成ボタン
- クイズ生成ボタン
- 生成済みスライド一覧
- 生成済みクイズ一覧
- 埋め込みコード
- 埋め込みコードのコピーボタン

### 表示イメージ

```txt
React Queryの記事

元記事URL:
https://example.com/articles/react-query

本文:
React Queryは...

生成状態:
スライド: 未生成
クイズ: 未生成

[スライドを生成する]
[クイズを生成する]

生成済みスライド:
1. React Queryとは
   サーバー状態を管理するための...

生成済みクイズ:
Q1. React Queryの主な役割は？
A. CSS管理
B. サーバー状態管理
C. ルーティング
D. 画像最適化

正解: B
解説: React Queryは...

埋め込みコード:
<div data-learning-widget data-article-id="article_xxx"></div>
<script src="https://your-service.com/embed.js"></script>

[コピー]
```

---

## 13.5 埋め込みウィジェット表示

### 表示場所

自分のブログの記事詳細ページ内。

### 要件

- スライド表示エリアを表示する
- クイズ表示エリアを表示する
- スライドは前へ・次へで移動できる
- クイズは選択肢をクリックして回答できる
- 回答後に正解・不正解を表示する
- 回答後に解説を表示する
- 読者の回答履歴は保存しない

---

## 14. 状態管理

### 14.1 生成状態

スライドとクイズの生成状態は別々に管理する。

```ts
type GenerationStatus = 'not_generated' | 'generating' | 'generated' | 'failed';
```

### 14.2 Article

```ts
type Article = {
  id: string;
  title: string;
  sourceUrl: string | null;
  sourceBody: string;
  slideStatus: GenerationStatus;
  quizStatus: GenerationStatus;
  createdAt: string;
  updatedAt: string;
};
```

---

## 15. データ設計

## 15.1 articles

```txt
id
title
source_url
source_body
slide_status
quiz_status
created_at
updated_at
```

### 補足

- `source_body` はブログ記事本文の正式な管理データではなく、AI生成に使うための本文コピーとして扱う
- ブログ記事と外部サービス側の記事本文は同期しない

---

## 15.2 slides

```txt
id
article_id
order
title
content
created_at
updated_at
```

### 補足

- `article_id` で articles に紐づく
- `order` で表示順を管理する
- 再生成時は対象記事の既存スライドを削除してから新規保存する

---

## 15.3 quizzes

```txt
id
article_id
order
question
choices
correct_choice_index
explanation
created_at
updated_at
```

### 補足

- `choices` は JSON として保存する
- `correct_choice_index` は `choices` の配列indexを指す
- MVPでは選択肢を別テーブルに分けない
- 再生成時は対象記事の既存クイズを削除してから新規保存する

---

## 16. API設計

## 16.1 管理用API

管理用APIは認証必須とする。

```txt
GET /api/articles
POST /api/articles
GET /api/articles/:id
PUT /api/articles/:id
DELETE /api/articles/:id

POST /api/articles/:id/generate-slides
POST /api/articles/:id/generate-quizzes
```

### GET /api/articles

登録済み記事一覧を取得する。

### POST /api/articles

記事を作成する。

Request:

```json
{
  "title": "React Queryの記事",
  "sourceUrl": "https://example.com/articles/react-query",
  "sourceBody": "React Queryは..."
}
```

### GET /api/articles/:id

記事詳細、生成済みスライド、生成済みクイズを取得する。

### PUT /api/articles/:id

記事タイトル、元記事URL、本文を更新する。

### DELETE /api/articles/:id

記事を削除する。

### POST /api/articles/:id/generate-slides

記事本文からスライドを生成する。

### POST /api/articles/:id/generate-quizzes

記事本文からクイズを生成する。

---

## 16.2 埋め込み表示用API

埋め込み表示用APIは認証なしでアクセス可能とする。

ただし、返却するのは公開可能な生成済みコンテンツのみとする。

```txt
GET /api/embed/articles/:id
```

### GET /api/embed/articles/:id

記事IDに紐づくスライド・クイズを取得する。

Response:

```json
{
  "articleId": "article_xxx",
  "title": "React Queryの記事",
  "slides": [
    {
      "order": 1,
      "title": "React Queryとは",
      "content": "React Queryはサーバー状態の取得・キャッシュ管理を扱いやすくするライブラリです。"
    }
  ],
  "quizzes": [
    {
      "order": 1,
      "question": "React Queryの主な役割は？",
      "choices": ["CSS管理", "サーバー状態管理", "ルーティング", "画像最適化"],
      "correctChoiceIndex": 1,
      "explanation": "React Queryはサーバー状態を扱うためのライブラリです。"
    }
  ]
}
```

---

## 17. AI生成仕様

## 17.1 スライド生成

### 入力

- 記事タイトル
- 記事本文

### 出力

AIは以下のJSON形式で返す。

```json
{
  "slides": [
    {
      "title": "React Queryとは",
      "content": "React Queryは、サーバー状態の取得・キャッシュ・再取得を扱いやすくするライブラリです。"
    },
    {
      "title": "導入メリット",
      "content": "ローディング状態、エラー状態、キャッシュ制御などをコンポーネント単位で扱いやすくできます。"
    }
  ]
}
```

### 生成ルール

- スライドは3〜6枚程度を目安にする
- 各スライドは短く読みやすくする
- 記事本文に書かれていない内容を過度に補完しない
- 技術的に不正確な内容を避ける
- 読者が記事全体の要点を復習できる内容にする

---

## 17.2 クイズ生成

### 入力

- 記事タイトル
- 記事本文

### 出力

AIは以下のJSON形式で返す。

```json
{
  "quizzes": [
    {
      "question": "React Queryの主な役割はどれですか？",
      "choices": ["CSSの管理を簡単にすること", "サーバー状態の取得・キャッシュ管理を扱いやすくすること", "ルーティングを自動生成すること", "画像最適化を行うこと"],
      "correctChoiceIndex": 1,
      "explanation": "React Queryは、サーバー状態の取得、キャッシュ、再取得、ローディング状態などを扱いやすくするライブラリです。"
    }
  ]
}
```

### 生成ルール

- クイズは3〜5問程度を目安にする
- 選択肢は4択にする
- 正解は1つにする
- `correctChoiceIndex` は `choices` の配列indexと一致させる
- 解説は記事本文に基づいた内容にする
- 記事本文に書かれていない内容を前提にした問題は避ける
- ひっかけ問題にしすぎない
- 学習定着を目的とした問題にする

---

## 18. 非機能要件

## 18.1 セキュリティ

- 管理画面は認証済みの自分のみアクセス可能にする
- 記事本文の登録・更新・削除・AI生成は管理者のみ実行可能にする
- AI APIキーはクライアント側に露出させない
- 埋め込み表示用APIでは、公開可能な生成済みコンテンツのみ返却する
- 管理用APIと埋め込み表示用APIを分離する

---

## 18.2 パフォーマンス

- 埋め込みスクリプトはブログページの表示を大きく妨げないようにする
- 埋め込み表示時にはAI生成を実行しない
- 生成済みコンテンツをDBから取得して表示する
- 埋め込み表示用APIは必要最小限のデータのみ返す
- embed.js は可能な範囲で軽量にする

---

## 18.3 保守性

- 記事登録処理、AI生成処理、埋め込み表示処理を分離する
- スライドとクイズは構造化して保存する
- 将来的に編集機能を追加できるようなデータ構造にする
- 将来的に複数ユーザー対応できるように、設計上は拡張余地を残す
- ただしMVPでは複雑な抽象化をしすぎない

---

## 18.4 表示互換性

- 埋め込みウィジェットはブログ側のCSS影響を受けにくい実装を目指す
- MVPでは通常のReact mountでよい
- CSS衝突が問題になった場合、Shadow DOM化を検討する

---

## 19. 技術構成案

### 19.1 管理サービス

```txt
Next.js App Router
TypeScript
Tailwind CSS
Drizzle ORM
Neon PostgreSQL
Auth.js
AI SDK または Gemini API / OpenAI API
```

### 19.2 埋め込みウィジェット

```txt
React
TypeScript
Vite または tsup
通常のReact mount
将来的に Shadow DOM
```

### 19.3 構成案

最初は1リポジトリ内で管理する。

```txt
src/app/admin
src/app/api
src/components
src/features
src/lib
src/embed
```

将来的に埋め込みウィジェットを独立ビルドしたくなった場合は、以下のようなモノレポ構成を検討する。

```txt
apps/web
  管理画面 + API

packages/embed
  埋め込みウィジェット

packages/shared
  型定義
```

---

## 20. MVPの成功条件

MVPでは以下を満たせば成功とする。

- 自分だけが管理画面にログインできる
- 記事タイトル・元記事URL・記事本文を登録できる
- 登録した記事から要約スライドを生成できる
- 登録した記事からクイズを生成できる
- 生成済みコンテンツを記事詳細画面で確認できる
- 埋め込みコードをコピーできる
- 自分のブログ記事ページに埋め込みコードを設置できる
- ブログ記事上でスライドを閲覧できる
- ブログ記事上でクイズに回答できる
- クイズ回答後に正誤と解説を確認できる

---

## 21. 実装優先度

### Phase 1: 土台作成

- Next.jsプロジェクト作成
- DB接続
- Drizzle設定
- articles / slides / quizzes テーブル作成
- 管理画面のレイアウト作成

### Phase 2: 記事管理

- 記事一覧画面
- 記事作成画面
- 記事詳細画面
- 記事更新
- 記事削除

### Phase 3: AI生成

- スライド生成API
- クイズ生成API
- 生成結果のJSONバリデーション
- 生成状態の管理
- 生成済みスライド・クイズの表示

### Phase 4: 埋め込み表示

- embed.js 作成
- data-learning-widget 要素の検出
- data-article-id の取得
- 埋め込み表示用API作成
- スライドUI表示
- クイズUI表示

### Phase 5: 認証・保護

- Googleログイン
- 自分のメールアドレスのみ許可
- 管理画面保護
- 管理用API保護

---

## 22. 開発時の注意点

- AI生成結果は必ずJSONとして扱える形にする
- AIレスポンスはZodなどでバリデーションする
- JSONパース失敗時のエラーハンドリングを行う
- 生成中はボタンをdisabledにする
- 再生成時は既存データを置き換える
- 埋め込み表示ではAI APIを呼ばない
- 埋め込み表示用APIでは記事本文を返さない
- クライアントにAI APIキーを露出させない
- MVPでは作り込みすぎず、まず一連の流れを通すことを優先する

---

## 23. このドキュメントに基づく実装指示

このドキュメントの内容に基づいて、MVPを実装する。

最初に重視することは、以下の一連の流れが動作することである。

```txt
記事本文を登録する
  ↓
スライドを生成する
  ↓
クイズを生成する
  ↓
埋め込みコードを取得する
  ↓
ブログ記事ページに埋め込み表示する
```

実装時は、過度に抽象化せず、MVPとしてシンプルな構成を優先する。
ただし、管理用APIと埋め込み表示用APIは分離し、AI APIキーや記事本文が不要に外部公開されないように注意する。
